const { getAllUsers, findUser, createUser, updateUser, deleteUser } = require('./user.controller');

module.exports = {
    getAllUsers,
    findUser,
    createUser,
    updateUser,
    deleteUser,
}
